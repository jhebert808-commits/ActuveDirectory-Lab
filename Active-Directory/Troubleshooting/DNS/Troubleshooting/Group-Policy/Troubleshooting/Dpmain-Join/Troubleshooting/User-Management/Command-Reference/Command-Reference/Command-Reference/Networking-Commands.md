# Networking Commands

## ping
Tests connectivity to another device.  
Use to confirm the client can reach the domain controller.

## nslookup
Queries DNS for name resolution.  
Use when domain join or GPO issues occur.

## tracert
Shows the path packets take to a destination.  
Use when diagnosing routing or network path issues.
